plugins {
    id "java-library"
}

dependencies {
    api(project(":blobstore-api"))
    api(project(":proxy-config"))

    api("com.zaxxer:HikariCP")
    api("org.slf4j:jul-to-slf4j")

    runtimeOnly("org.glassfish.jaxb:jaxb-runtime")
    runtimeOnly("org.hsqldb:hsqldb")
}
